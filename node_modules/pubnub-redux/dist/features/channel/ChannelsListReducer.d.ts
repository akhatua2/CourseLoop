/// <reference types="pubnub" />
import { AllChannelDataRetrievedAction } from './ChannelDataActions';
import { ActionMeta, AnyMeta } from 'foundations/ActionMeta';
interface ChannelListState {
    channelIds: string[];
}
export declare const createChannelsListReducer: <ChannelCustom extends import("pubnub").ObjectCustom = import("pubnub").ObjectCustom, Meta extends ActionMeta = AnyMeta>() => (state: ChannelListState | undefined, action: AllChannelDataRetrievedAction<ChannelCustom, Meta>) => ChannelListState;
export {};
